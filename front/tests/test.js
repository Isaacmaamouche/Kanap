describe("Calculator tests", () => {
  test("adding 1 + 2 should return 3", () => {
    expect(3).toBe(3);
  });
  test("adding 1 + 2 should return 3", () => {
    expect(3).toBe(3);
  });
});

describe("Calculator tests", () => {
  test("adding 1 + 2 should return 3", () => {
    expect(3).toBe(3);
  });
  test("adding 1 + 2 should return 3", () => {
    expect(3).toBe(3);
  });
});
